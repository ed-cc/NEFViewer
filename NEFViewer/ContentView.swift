import SwiftUI

// Phase 7 placeholder — full folder browser implemented in Phase 7.
struct ContentView: View {
    var body: some View {
        Text("NEFViewer")
            .font(.largeTitle)
            .foregroundColor(.secondary)
            .frame(minWidth: 600, minHeight: 400)
    }
}
